void exportData(dynamic data) {
  // On non-web platforms, you could optionally save to a file or just do nothing.
  // For now, this function is a no-op. The UI fallback will show a dialog with the data.
}